{
  "version": 2,
  "regions": ["sin1"],
  "buildCommand": "prisma generate && prisma db push",
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "outputDirectory": "public",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    },
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
